$('#order_item_<%=@item.id%>').hide();
$('#total-amount').html("Your Total: <%= number_to_currency current_order.total_price %>");
$('#total-cart-items').html('<%= link_to "Cart | Total items: #{ cart_items }", cart_path %>');
